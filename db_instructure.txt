orders:
    order_id
    symbol
    arrive_time
    send_time
    type
    size
    price
    slippage
    stoploss
    takeprofit
    comment

SQL: CREATE TABLE `orders` (
  `order_id` int(11) DEFAULT NULL,
  `arrive_time` varchar(128) DEFAULT NULL,
  `symbol` varchar(128) DEFAULT NULL,
  `sent_time` varchar(32) DEFAULT NULL,
  `type` varchar(20480) DEFAULT NULL,
  `size` varchar(64) DEFAULT NULL,
  `slippage` varchar(128) DEFAULT NULL,
  `stoploss` varchar(128) DEFAULT NULL,
  `takeprofit` varchar(128) DEFAULT NULL,
  `taxes` varchar(128) DEFAULT NULL,
  `swap` varchar(128) DEFAULT NULL,
  `comment` varchar(128) DEFAULT NULL,
  PRIMARY KEY (`order_id`)
) ENGINE=InnoDB AUTO_INCREMENT=0 DEFAULT CHARSET=utf8;
